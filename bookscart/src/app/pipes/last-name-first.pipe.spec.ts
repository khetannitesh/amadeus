import { LastNameFirstPipe } from './last-name-first.pipe';

describe('LastNameFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new LastNameFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
